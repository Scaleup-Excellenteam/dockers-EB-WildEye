version: '3.8'

services:
  router:
    build: ./router
    ports:
      - "5004:5000"
    networks:
      - code-net

  python-executor:
    build: ./python-executor
    networks:
      - code-net

  java-executor:
    build: ./java-executor
    ports:
      - "5002:5002"
    networks:
      - code-net

  dart-executor:
    build: ./dart-executor
    ports:
      - "5003:5003"
    networks:
      - code-net

networks:
  code-net:
    driver: bridge